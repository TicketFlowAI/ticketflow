import { ScrollToTopSmoothDirective } from './scroll-to-top-smooth.directive';

describe('ScrollToTopSmoothDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToTopSmoothDirective();
    expect(directive).toBeTruthy();
  });
});
