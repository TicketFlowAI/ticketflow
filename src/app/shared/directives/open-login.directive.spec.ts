import { OpenLoginDirective } from './open-login.directive';

describe('OpenLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenLoginDirective();
    expect(directive).toBeTruthy();
  });
});
